
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function RiskScoringPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Risk Scoring Dashboard</CardTitle>
        <CardDescription>View and analyze risk scores for customers and transactions.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>A dashboard with risk scoring analytics and visualizations will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
