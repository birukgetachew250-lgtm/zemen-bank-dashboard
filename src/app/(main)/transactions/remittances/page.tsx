
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function RemittancesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Remittances</CardTitle>
        <CardDescription>Monitor inbound and outbound remittances.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Content for remittances will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
