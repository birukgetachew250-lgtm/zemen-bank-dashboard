
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function AmlKycPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>AML / KYC Flags & Reports</CardTitle>
        <CardDescription>Monitor Anti-Money Laundering (AML) and Know Your Customer (KYC) compliance flags and generate reports.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Content for AML/KYC flags and reporting will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
