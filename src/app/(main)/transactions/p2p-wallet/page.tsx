
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function P2PWalletTransfersPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>P2P & Wallet Transfers</CardTitle>
        <CardDescription>Monitor peer-to-peer and internal wallet transfers.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Content for P2P and wallet transfers will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
