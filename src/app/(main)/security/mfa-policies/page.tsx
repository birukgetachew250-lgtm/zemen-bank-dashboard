
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function MfaPoliciesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>MFA / Security Policies</CardTitle>
        <CardDescription>Configure Multi-Factor Authentication requirements and other security policies.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Configuration options for MFA and security policies will be available here.</p>
      </CardContent>
    </Card>
  );
}
