
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function CustomReportsBuilderPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Custom Reports Builder</CardTitle>
        <CardDescription>Create and save your own custom reports by selecting metrics and dimensions.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>A tool for building custom reports will be available here.</p>
      </CardContent>
    </Card>
  );
}
